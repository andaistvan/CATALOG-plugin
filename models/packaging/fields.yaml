# ===================================
#  Form Field Definitions
# ===================================

fields:
    id:
        label: ID
        # disabled: true

    packaging_name:
        label: Kiszerelés
        span: left

    slug:
        label: Slug
        span: right
        preset:
            field: packaging_name
            type: slug
